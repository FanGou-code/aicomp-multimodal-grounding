# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Conda
.conda/

# IDE
.idea/
.vscode/
.qoder/
*.swp
*.swo

# Environment & secrets
.env
.env.*
*.pem
*.key

# Data files & Datasets (shared offline via ModelScope, never committed)
data/

# Model weights & LoRA adapters (any location)
# NOTE: *.bin / *.pt are intentionally broad — weight files of any naming land here.
*.safetensors
*.bin
*.pt
*.pth
*.gguf
model/
best/
checkpoints/

# OS / WSL
.DS_Store
Thumbs.db
*:Zone.Identifier

# Outputs: local only. Every generated artifact (annotations, LoRA adapters,
# predictions, submissions) stays out of the repository.
outputs/

# Internal working notes: kept in the local checkout, never published.
docs/handoff*.md
*handoff*.md
AGENTS.md

