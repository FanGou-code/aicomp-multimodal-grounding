# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Conda
.conda/

# IDE
.idea/
.vscode/
.qoder/
*.swp
*.swo

# Environment & secrets
.env
.env.*
*.pem
*.key

# Data files & Datasets (shared offline via ModelScope, never committed)
data/

# Model weights & LoRA adapters (any location)
# NOTE: *.bin / *.pt are intentionally broad — weight files of any naming land here.
*.safetensors
*.bin
*.pt
*.pth
*.gguf
model/
best/
checkpoints/

# OS / WSL
.DS_Store
Thumbs.db
*:Zone.Identifier

# Outputs: everything is local EXCEPT the golden annotation set, which is the
# shared training contract and travels with the repository (update the run id
# below deliberately when a new golden annotation run is published).
outputs/**
!outputs/annotations/
outputs/annotations/**
!outputs/annotations/annot_dc189f029d962b27/
!outputs/annotations/annot_dc189f029d962b27/train/
!outputs/annotations/annot_dc189f029d962b27/val/
!outputs/annotations/annot_dc189f029d962b27/train/approved.json
!outputs/annotations/annot_dc189f029d962b27/val/approved.json
