# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Conda
.conda/

# IDE
.idea/
.vscode/
.qoder/
*.swp
*.swo

# Environment & secrets
.env
.env.*
*.pem
*.key
*api_keys.txt
keys/

# Caches & coverage
.ruff_cache/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Data files & Datasets: never committed, never distributed.
data/

# Model weights & LoRA adapters (any location)
# NOTE: *.bin / *.pt are intentionally broad — weight files of any naming land here.
*.safetensors
*.bin
*.pt
*.pth
*.gguf
model/
best/
checkpoints/

# OS / WSL
.DS_Store
Thumbs.db
*:Zone.Identifier

# Outputs: default ignore all, whitelist only the two active annotation datasets.
outputs/*
!outputs/annotations/
outputs/annotations/*
!outputs/annotations/train.json
!outputs/annotations/val.json

# docs/: whitelist mode — only data-contract.md is published.
docs/*
!docs/data-contract.md

# Internal working notes: never published.
AGENTS.md

# Platform-specific tooling kept in the local checkout, never published:
# the repository itself stays platform-neutral.
local/
