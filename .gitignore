# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Conda
.conda/

# IDE
.idea/
.vscode/
.qoder/
*.swp
*.swo

# Environment & secrets
.env
.env.*
*.pem
*.key
*api_keys.txt
keys/

# Caches & coverage
.ruff_cache/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Data files & Datasets: never committed, never distributed.
data/

# Model weights & LoRA adapters (any location)
# NOTE: *.bin / *.pt are intentionally broad — weight files of any naming land here.
*.safetensors
*.bin
*.pt
*.pth
*.gguf
model/
best/
checkpoints/

# OS / WSL
.DS_Store
Thumbs.db
*:Zone.Identifier

# Outputs: local only. Every generated artifact (annotations, LoRA adapters,
# predictions, submissions) stays out of the repository.
outputs/

# Internal working notes and platform runbook: kept in the local checkout,
# never published (README + docs/ carry everything an external user needs).
*handoff*.md
docs/sop*.md
AGENTS.md

# Platform-specific tooling kept in the local checkout, never published:
# the repository itself stays platform-neutral.
local/
